type Activity = {
  componentId?: number;
  activityName: string;
  startDate: string;
  endDate: string;
  inProgress: boolean;
  link?: string;
  description?: string;
};

export type { Activity };
