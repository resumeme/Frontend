type Activity = {
  componentId?: string;
  activityName: string;
  startDate: string;
  endDate: string;
  inProgress: boolean;
  link?: string;
  description?: string;
};

export type { Activity };
