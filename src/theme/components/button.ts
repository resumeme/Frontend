import { defineStyleConfig } from '@chakra-ui/react';

const Button = defineStyleConfig({
  sizes: {
    lg: {
      w: '25.25rem',
    },
    md: {
      w: '10rem',
    },
    sm: {
      w: '5.125rem',
    },
    xs: {
      w: '3.16rem',
    },
  },
});
export { Button };
